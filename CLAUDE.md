# Local Instructions

## README files

Always leave README files with no markdownlint warnings or errors.

## Package manager

Always use yarn over npm for all package operations (`yarn install`, `yarn add`, `yarn <script>`).
